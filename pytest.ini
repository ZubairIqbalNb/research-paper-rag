[pytest]
# Make both the project root (health.py, backend/) and tests/ (test doubles)
# importable regardless of how pytest is invoked.
pythonpath = . tests
testpaths = tests
markers =
    slow: end-to-end tests that load the real Sentence Transformer model
