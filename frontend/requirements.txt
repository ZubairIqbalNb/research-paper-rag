# Subset of root requirements.txt — keep versions in sync manually on upgrade.
# Slim, deploy-time list for the Streamlit frontend (Streamlit Community Cloud).
# For local development install the ROOT requirements.txt instead.
#
# Phase 5 frontend. The Streamlit app is a thin HTTP client over the FastAPI
# backend; the packages below are everything imported under frontend/.
streamlit==1.64.0
httpx==0.28.1
